import { FilterSaleItemsPipe } from './filter-sale-items.pipe';

describe('FilterSaleItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSaleItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
